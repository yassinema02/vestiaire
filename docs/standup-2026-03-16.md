## Weekly Standup Record — Vestiaire

**Week of:** March 10 – March 16, 2026
**Project:** Vestiaire (React Native + Expo, Supabase)

---

### What was accomplished this week

No new commits were made this week. The last commit was on **March 1** (Epic 13 completion). Current work is focused on **pre-launch stabilization**, not new features.

**Uncommitted work in progress** (files modified but not yet committed):
- New CI workflow: `.github/workflows/typecheck.yml`
- New shared UI component: `AuthScreenShell.tsx`
- New utilities: `runtimeConfig.ts`, `uiStore.ts`, `theme/tokens.ts`
- New hook: `useTabBarOnScroll.ts`
- Jest config added: `apps/mobile/jest.config.js`
- Documentation: codebase upgrade execution plan, current architecture, target architecture, launch scope, deferred features, project status board

### Project status at a glance

| Area | Status |
|------|--------|
| **Epics 1–9** (Core, Social, Shopping) | Done |
| **Epics 10–13** (Analytics, Calendar, Resale) | Done |
| **Production hardening** | In Progress |
| **Test infrastructure** | In Progress (Jest config added, no CI yet) |
| **Push notifications** | Deferred (requires dev build, not Expo Go) |
| **Premium billing** | Simulated (hide if still fake at launch) |
| **AI security** | Known risk (client-side API keys) |

### Completed feature count
- **28 screens**, **40+ components**, **35+ services**, **5 stores**, **120+ source files**
- **38 stories** completed across **10 epics** (4–13)

### What's in progress / planned next
1. **Launch hardening** — stabilize auth, wardrobe, outfit core flows
2. **Architecture cleanup** — modularize oversized screens/services, enforce theme tokens
3. **CI/CD setup** — typecheck workflow added, Jest config in place but tests not running yet
4. **Known critical bugs to fix:**
   - `authStore` memory leak (unsubscribed listener)
   - `incrementWearCount` race condition (non-atomic)
   - No timeouts on external API calls
   - Stale closures in `log-wear.tsx`, `outfits/swipe.tsx`

### Blockers
- **Push notifications** blocked on Expo Go limitations — needs EAS/dev build
- **Jest** config exists but tests aren't passing yet (no transform for TS/ESM)

### Risks
- No automated test coverage running in CI
- Large uncommitted diff growing without commits

### What I want to do next

Deployed Supabase Edge Functions (`ai-proxy`, `analyze-product`, `notify-ootd-post`) and migrated all 9 AI services from client-side Gemini calls to server-side proxy, securing API keys off-device. Upgraded all AI calls from `gemini-2.0-flash` (deprecated) to `gemini-2.5-flash`. Next step is to verify all AI-powered features end-to-end (outfit generation, categorization, shopping analysis, etc.) through the new proxy, then commit this stabilization work and move on to fixing the critical bugs (authStore leak, non-atomic wear count, stale closures).
